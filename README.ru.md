# Автономный ИИ-агент управления и динамического репрайсинга для Ozon

Профессиональная модульная система автоматизации бизнес-процессов и контроля финансовых рисков на маркетплейсе Ozon (институциональный уровень). Платформа в реальном времени рассчитывает сквозную юнит-экономику, осуществляет конкурентную разведку (модуль OSINT), жестко контролирует лимиты маржинальности и генерирует поисковую SEO-оптимизацию карточек с помощью встроенных алгоритмов ИИ.

## Основные архитектурные модули

1. **Ядро юнит-экономики (Quant Core)**: Осуществляет непрерывный расчет чистой прибыли, маржинальности и окупаемости инвестиций (ROI) для каждого SKU. На лету учитывает себестоимость, комиссии маркетплейса, многоуровневую логистику (тарифы FBO) и налоговые ставки.
2. **OSINT-мониторинг и динамическое ценообразование (Dynamic Re-pricing)**: Имитирует высокочастотный скан цен конкурентов. Автоматически запускает протокол демпинга (`MATCH_COMPETITOR_LOWER`), удерживая карточку в топе выдачи. Если конкуренты опускают цену ниже критической отметки, срабатывает защитный алгоритм `HOLD_MIN_MARGIN_LIMIT`, намертво блокирующий цену для защиты минимальной нормы прибыли.
3. **ИИ-директор (Executive Decision Engine)**: Анализирует оборачиваемость запасов и финансовые показатели, формируя для владельца бизнеса оперативные стратегические директивы по закупкам, поставкам на склады и управлению маркетинговым бюджетом.
4. **ИИ-фабрика контента (SEO Copywriting Factory)**: Автоматически генерирует оптимизированные под поисковые алгоритмы маркетплейса описания товаров и релевантные поисковые теги (метаданные).

## Математический аппарат

Вычислительное ядро юнит-экономики использует следующую целевую функцию для расчета чистой прибыли:

$$\text{Net Profit} = P_{current} \cdot (1 - \tau) - C_{cost} - (P_{current} \cdot \mu_{ozon}) - L_{fbo}$$

Где:
- $P_{current}$: Текущая розничная цена SKU на маркетплейсе.
- $\tau$: Ставка налогообложения (по умолчанию: $0.06$ для УСН «Доходы»).
- $C_{cost}$: Базовая себестоимость единицы товара.
- $\mu_{ozon}$: Динамический коэффициент комиссии Ozon для конкретной категории товара.
- $L_{fbo}$: Фиксированные операционные расходы на логистику, сборку и магистраль FBO.

Нижний порог безопасной цены ($P_{min}$), гарантирующий защиту целевой маржинальности, рассчитывается по формуле:

$$P_{min} = \frac{C_{cost} + L_{fbo}}{1 - \mu_{ozon} - \tau - \text{Margin}_{min}}$$

## Развертывание и запуск проекта

### Системные требования
- Установленный Docker и Docker Compose.

### Быстрый запуск (сборка и старт в бэкграунде)
Просто скопируйте и вставьте эту команду в терминал внутри папки проекта:

```bash
docker compose up --build -d
```

## После успешного запуска веб-интерфейс дашборда будет доступен в вашем браузере по адресу: [http://localhost:8501](http://localhost:8501).

## Структура проекта

```text
ozon_smart_agent/
│
├── core/
│   └── analytics.py        # Вычислительное ядро, симулятор OSINT и ИИ-генератор
├── data/
│   └── portfolio_ozon.json  # Центральный реестр SKU, матрица затрат и лимитов
├── app.py                  # Управляющий дашборд на Streamlit
├── requirements.txt        # Манифест зависимостей Python
├── Dockerfile              # Конфигурация сборки Docker-контейнера
└── docker-compose.yml      # Оркестратор локальной виртуализации инфраструктуры
```

## Полезные команды для управления контейнером

- Посмотреть статус контейнера:

```bash
docker compose ps
```

- Посмотреть логи приложения в реальном времени:

```bash
docker compose logs -f ozon-agent
```

- Остановить и удалить контейнер:

```bash
docker compose down
```

## Конфигурация бизнес-правил:
[portfolio_ozon.json](core/data/portfolio_ozon.json)

## Вы можете изменять параметры товаров, себестоимость, идентификаторы конкурентов и защитные лимиты маржи напрямую в конфигурационном JSON-файле:

```JSON
{
  "currency": "RUB",
  "tax_rate": 0.06,
  "products": {
    "SKU-IDENTIFIER": {
      "name": "Название товара",
      "stock": 50,
      "cost_price": 500.0,
      "current_price": 1500.0,
      "ozon_commission_pct": 0.15,
      "logistics_fbo": 100.0,
      "min_margin_pct": 0.20,
      "target_competitor_sku": "COMP-ID-01"
    }
  }
}
```





